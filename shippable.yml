language: php

php:
  - "7.1"

build:
  ci:
    - cd lara
    - composer global require "laravel/installer"
    - export PATH=~/.composer/vendor/bin:$PATH
    - laravel new blog
    - php artisan serve
